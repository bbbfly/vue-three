# Vue-Three 组件库 产品需求文档

## 1. 产品概述

Vue-Three 是一个基于 Three.js 封装的 Vue 3 组件库，旨在为企业团队提供一套标准化、易维护、高性能的 3D 可视化开发方案。通过配置式 API 设计，降低 3D 开发门槛，提升开发效率，统一团队开发规范。

### 1.1 产品定位

- 面向企业级应用的 3D 可视化组件库
- 基于 Vue 3 + TypeScript，提供完整类型支持
- 配置式驱动，声明式渲染，降低 3D 开发学习成本
- 内置 Playground 演示平台，支持实时调试和属性配置

### 1.2 设计原则

1. **配置优先**：通过单一配置对象驱动场景创建，便于序列化和存储
2. **类型安全**：完整的 TypeScript 类型推导，开发时即发现错误
3. **性能优化**：按需引入，Tree Shaking 支持，最小化打包体积
4. **企业友好**：稳定 API，完善文档，长期维护支持

---

## 2. 目标用户场景

### 2.1 核心用户：企业开发团队

- **场景1：数据可视化大屏**
  - 用户：前端开发团队
  - 需求：快速构建 3D 数据看板、园区模型、数字孪生场景
  - 痛点：Three.js 学习成本高，团队成员水平不一，代码风格难统一

- **场景2：产品 3D 展示**
  - 用户：电商/制造业前端团队
  - 需求：产品 3D 模型展示、720度全景预览、装配动画
  - 痛点：模型加载慢、动画调试难、兼容性问题多

- **场景3：教育培训平台**
  - 用户：在线教育平台开发团队
  - 需求：3D 教学模型、交互式演示、实验模拟
  - 痛点：物理模拟复杂、交互开发难度大、性能优化困难

---

## 3. 核心功能

### 3.1 核心组件系统

#### 3.1.1 场景基础组件

| 组件名          | 功能描述                                 |
| --------------- | ---------------------------------------- |
| `TCanvas`       | 画布容器，负责场景初始化、渲染循环管理   |
| `TScene`        | 场景容器，管理整个 3D 场景的配置         |
| `TAssetManager` | 资源管理器，统一管理纹理、模型等资源加载 |

#### 3.1.2 几何体组件

| 组件名            | 功能描述         |
| ----------------- | ---------------- |
| `TBox`            | 立方体几何体     |
| `TSphere`         | 球体几何体       |
| `TPlane`          | 平面对象         |
| `TCylinder`       | 圆柱体几何体     |
| `TTorus`          | 圆环几何体       |
| `TCone`           | 圆锥几何体       |
| `TCustomGeometry` | 自定义几何体支持 |

#### 3.1.3 曲线与路径组件

| 组件名                  | 功能描述         |
| ----------------------- | ---------------- |
| `TArcCurve`             | 圆弧曲线         |
| `TEllipseCurve`         | 椭圆曲线         |
| `TBezierCurve`          | 三次贝塞尔曲线   |
| `TQuadraticBezierCurve` | 二次贝塞尔曲线   |
| `TSplineCurve`          | 样条曲线         |
| `TCatmullRomCurve`      | Catmull-Rom 曲线 |
| `TLine`                 | 线条渲染组件     |
| `TLineLoop`             | 闭环线条         |
| `TLineDashed`           | 虚线线条         |

#### 3.1.4 高级几何体组件

| 组件名               | 功能描述                            |
| -------------------- | ----------------------------------- |
| `TTubeGeometry`      | 曲线路径管道（沿曲线创建管道）      |
| `TLatheGeometry`     | 旋转成型（绕Y轴旋转轮廓创建几何体） |
| `TExtrudeGeometry`   | 拉伸几何体（将2D轮廓拉伸为3D）      |
| `TShapeGeometry`     | 轮廓填充（将2D形状填充为面）        |
| `TSweepGeometry`     | 扫描几何体（沿路径扫描截面轮廓）    |
| `TEdgesGeometry`     | 模型边界线（提取几何体边缘）        |
| `TWireframeGeometry` | 线框几何体                          |

#### 3.1.6 材质纹理组件

| 组件名                  | 功能描述               |
| ----------------------- | ---------------------- |
| `TMeshBasicMaterial`    | 基础网格材质           |
| `TMeshStandardMaterial` | PBR标准材质            |
| `TMeshPhysicalMaterial` | 增强型PBR物理材质      |
| `TMeshLambertMaterial`  | Lambert材质            |
| `TMeshPhongMaterial`    | Phong材质              |
| `TTexture`              | 纹理贴图组件           |
| `TCubeTexture`          | 立方体贴图（环境贴图） |

#### 3.1.7 光源系统组件

| 组件名              | 功能描述 |
| ------------------- | -------- |
| `TAmbientLight`     | 环境光   |
| `TDirectionalLight` | 方向光   |
| `TPointLight`       | 点光源   |
| `TSpotLight`        | 聚光灯   |
| `THemisphereLight`  | 半球光   |
| `TRectAreaLight`    | 区域光   |

#### 3.1.8 相机控制组件

| 组件名                 | 功能描述       |
| ---------------------- | -------------- |
| `TPerspectiveCamera`   | 透视相机       |
| `TOrthographicCamera`  | 正交相机       |
| `TOrbitControls`       | 轨道控制器     |
| `TFlyControls`         | 飞行控制器     |
| `TFirstPersonControls` | 第一人称控制器 |

#### 3.1.9 模型加载组件

| 组件名         | 功能描述           |
| -------------- | ------------------ |
| `TGLTFLoader`  | GLTF/GLB 模型加载  |
| `TOBJLoader`   | OBJ 模型加载       |
| `TFBXLoader`   | FBX 模型加载       |
| `TDRACOLoader` | DRACO 压缩模型解码 |

#### 3.1.10 动画系统组件

| 组件名               | 功能描述     |
| -------------------- | ------------ |
| `TAnimationMixer`    | 动画混合器   |
| `TKeyframeAnimation` | 关键帧动画   |
| `TTransition`        | 过渡动画     |
| `TMorphAnimation`    | 变形目标动画 |

#### 3.1.11 后期处理组件

| 组件名              | 功能描述       |
| ------------------- | -------------- |
| `TEffectComposer`   | 效果合成器     |
| `TBloomPass`        | Bloom 发光效果 |
| `TSSAAPass`         | 抗锯齿         |
| `TDepthOfFieldPass` | 景深效果       |
| `TColorGrading`     | 色彩分级       |

#### 3.1.12 物理引擎组件

| 组件名          | 功能描述   |
| --------------- | ---------- |
| `TPhysicsWorld` | 物理世界   |
| `TRigidBody`    | 刚体组件   |
| `TCollider`     | 碰撞体组件 |
| `TConstraint`   | 约束组件   |

#### 3.1.13 交互事件系统

| 组件名 / Composable | 功能描述                                   |
| ------------------- | ------------------------------------------ |
| `useInteraction`    | 交互管理核心，射线检测 + 事件派发          |
| `TMesh`             | 网格组件支持 @click / @pointer-\* 系列事件 |
| `TGroup`            | 组对象支持交互事件                         |
| `TLine`             | 线条对象支持交互事件                       |
| `TGLTFModel`        | 加载模型支持交互事件                       |

#### 3.1.14 CSS2D 标签渲染系统

| 组件名 / Composable | 功能描述                                                       |
| ------------------- | -------------------------------------------------------------- |
| `useCSS2DRenderer`  | CSS2D 渲染器核心 composable，管理 CSS2DRenderer 实例与渲染循环 |
| `TCSS2DRenderer`    | CSS2D 渲染器组件，初始化并注入上下文                           |
| `TCSS2DLabel`       | 2D 标签组件，基于 HTML 元素，始终面向相机                      |
| `TCSS2DObject`      | CSS2D 通用对象，支持自定义 HTML 内容                           |

**CSS2D 核心特性**：

- ✅ 纯 HTML/CSS 渲染，支持完整 CSS 样式
- ✅ 始终面向相机（自动 billboard 效果）
- ✅ 完美与 3D 场景坐标对齐
- ✅ 支持所有 HTML 事件（click、hover 等）
- ✅ 支持透明度、z-index 层级控制
- ✅ 可配置距离显示范围（近距显示/远距隐藏）
- ✅ 支持缩放随距离衰减（近大远小效果）
- ✅ 支持偏移量配置（避免与 3D 物体重叠）

#### 3.1.15 Sprite 精灵模型组件

| 组件名 / Composable | 功能描述                                           |
| ------------------- | -------------------------------------------------- |
| `useSprite`         | Sprite 核心 composable，管理精灵实例与资源生命周期 |
| `TSprite`           | 精灵组件，面向相机的 2D 平面渲染对象               |
| `TSpriteMaterial`   | 精灵材质组件，支持纹理与颜色配置                   |

**Sprite 核心特性**：

- ✅ 始终面向相机（自动 Billboard 效果）
- ✅ 支持纹理贴图加载与显示
- ✅ 支持纯色填充模式
- ✅ 支持透明度与 Alpha 通道
- ✅ 可配置尺寸（width / height / scale）
- ✅ 支持中心点对齐配置（center）
- ✅ 支持 UV 坐标偏移与重复
- ✅ 支持颜色叠加 tint 效果
- ✅ 可配置距离显示范围（近距显示/远距隐藏）
- ✅ 支持像素级渲染（大小不受透视影响）
- ✅ 支持交互事件（click / pointer 系列事件）
- ✅ 深度写入 / 深度测试配置
- ✅ 圆形裁剪 / 圆角效果支持
- ✅ 资源自动清理与内存管理

**典型应用场景**：

1. **标记点系统**：地图 / 场景中的 POI 点、热点标记
2. **粒子系统**：2D 精灵粒子、烟雾、火焰效果
3. **公告牌**：人物头顶名称、状态图标、信息提示
4. **UI 元素**：3D 空间中的 2D 界面元素、按钮
5. **特效系统**：光晕、光斑、星空背景

---

### 3.2 Playground 演示平台

#### 3.2.1 界面布局

- **布局风格**：全屏预览 + 侧边配置
- **左侧边栏**：
  - 组件目录树：按分类展示所有可用组件
  - 属性配置面板：选中组件的实时属性编辑
  - GUI 调试器：基于 Three.js GUI 的可视化调参
- **主预览区**：
  - 全屏 3D 场景渲染
  - 实时响应配置变更
  - 场景交互操作
- **底部/右侧面板**（可折叠）：
  - 配置代码生成展示
  - 一键复制可用代码
  - 预设配置快速切换

#### 3.2.2 核心功能

1. **组件演示**：每个组件提供独立演示场景
2. **实时调试**：通过 GUI 实时修改组件属性
3. **配置导出**：调试完成的配置可导出为 JSON/代码
4. **场景快照**：保存当前场景状态，支持快速恢复
5. **代码示例**：每个示例提供完整的 Vue 组件代码
6. **响应式预览**：支持不同屏幕尺寸下的效果预览

---

## 4. 功能优先级

### 4.1 MVP 版本（v1.0.0）

**必须完成**：

- [x] 项目基础框架搭建
- [x] TCanvas 核心画布组件
- [x] TScene 场景配置组件
- [x] 基础几何体组件（Box, Sphere, Plane, Cylinder）
- [x] 基础材质组件
- [x] 常用光源组件
- [x] 透视相机 + 轨道控制器
- [x] GLTF 模型加载
- [x] Playground 基础框架
- [x] 基础组件演示页面
- [x] GUI 属性调试集成
- [x] TypeScript 完整类型支持
- [x] 按需引入 + Tree Shaking 支持

### 4.2 迭代版本（v1.1.0）

**计划完成**：

- [ ] 动画系统核心组件
- [ ] 更多几何体和材质类型
- [ ] 后期处理基础效果（Bloom、抗锯齿）
- [ ] OBJ + FBX 模型加载器
- [ ] Playground 配置导出功能
- [ ] 组件文档完善
- [ ] 性能优化：实例化渲染、LOD

### 4.3 交互事件系统（v1.2.0）

**计划完成**：

- [ ] 全局 Raycaster 射线检测管理
- [ ] TMesh 组件支持 click / pointerenter / pointerleave 事件
- [ ] 支持事件冒泡与 stopPropagation
- [ ] 交互事件完整类型定义
- [ ] 性能优化：只检测已注册交互的对象
- [ ] TGroup / TLine / TModel 支持交互事件
- [ ] 支持鼠标悬停高亮效果
- [ ] Playground 交互事件演示页面

### 4.4 CSS2D 标签渲染系统（v1.4.0）

**计划完成**：

- [ ] CSS2DRenderer 渲染器集成
- [ ] useCSS2DRenderer composable 核心实现
- [ ] TCSS2DRenderer 根组件
- [ ] TCSS2DLabel 标签组件（标题、注释、标注）
- [ ] TCSS2DObject 通用对象（自定义 HTML）
- [ ] 距离显示范围控制（minDistance / maxDistance）
- [ ] 缩放随距离衰减（scaleByDistance）
- [ ] 像素偏移量配置（中心偏移、防重叠）
- [ ] 支持 HTML 原生事件
- [ ] CSS2D Playground 演示页面
- [ ] 标签样式 GUI 调试

### 4.5 曲线与高级几何体（v1.5.0）

**计划完成**：

- [ ] 曲线组件（圆弧、椭圆、贝塞尔、样条曲线）
- [ ] 线条渲染组件（实线、闭环、虚线）
- [ ] 曲线路径管道（TubeGeometry）
- [ ] 旋转成型（LatheGeometry）
- [ ] 轮廓填充（ShapeGeometry）
- [ ] 拉伸几何体（ExtrudeGeometry）
- [ ] 扫描几何体（SweepGeometry）
- [ ] 模型边界线与线框（Edges、Wireframe）
- [ ] 曲线与高级几何体 Playground 演示页面
- [ ] 曲线编辑器 GUI 工具

### 4.4 高级版本（v1.3.0+）

**未来规划**：

- [ ] 物理引擎集成
- [ ] 完整后期处理管线
- [ ] 自定义着色器支持
- [ ] VR/AR 支持
- [ ] 更多预设场景模板
- [ ] 企业级主题定制

---

## 5. 界面设计要求

### 5.1 Playground 界面

1. **现代简约**：深色主题，突出 3D 场景内容
2. **分区清晰**：功能区域划分明确，操作流程直观
3. **沉浸式预览**：最大化 3D 场景可视区域
4. **可定制面板**：侧边栏可折叠、可调整宽度
5. **响应式设计**：适配不同屏幕尺寸
6. **视觉反馈**：配置变更时提供即时视觉反馈

### 5.2 视觉风格

- 配色：深色背景 + 科技蓝主色调
- 字体：现代无衬线字体，保证代码可读性
- 动效：平滑过渡动画，避免干扰 3D 场景
- 图标：简洁线性图标，功能表意清晰

---

## 6. 技术栈建议

### 6.1 核心技术

| 技术            | 选型说明                           |
| --------------- | ---------------------------------- |
| Vue 3           | Composition API + `<script setup>` |
| TypeScript      | 严格模式，完整类型推导             |
| Three.js        | 最新稳定版，核心渲染引擎           |
| Vite            | 开发服务器 + 构建工具              |
| Vitest          | 单元测试框架                       |
| Vue Router      | Playground 路由管理                |
| Pinia           | Playground 状态管理                |
| UnoCSS/Tailwind | Playground 样式方案                |

### 6.2 构建配置

- 输出格式：ES Module + UMD
- 按需引入：Tree Shaking 优化
- 外部化：Three.js 作为 peerDependency
- 类型声明：单独输出 d.ts 文件
- 压缩混淆：生产环境代码优化

---

## 7. 代码风格和架构模式

### 7.1 架构设计

```
packages/
├── vue-three/              # 组件库核心
│   ├── src/
│   │   ├── core/           # 核心引擎封装
│   │   ├── components/     # 组件实现
│   │   │   ├── geometries/
│   │   │   ├── materials/
│   │   │   ├── lights/
│   │   │   ├── cameras/
│   │   │   ├── loaders/
│   │   │   ├── animation/
│   │   │   ├── postprocessing/
│   │   │   └── physics/
│   │   ├── composables/    # 组合式函数
│   │   ├── utils/          # 工具函数
│   │   ├── types/          # 类型定义
│   │   └── index.ts        # 入口文件
│   └── package.json
└── playground/             # 演示平台
    ├── src/
    │   ├── views/          # 演示页面
    │   ├── components/     # 演示平台UI组件
    │   ├── router/         # 路由配置
    │   ├── stores/         # 状态管理
    │   └── examples/       # 示例配置
    └── package.json
```

### 7.2 设计模式

1. **配置驱动模式**：所有组件通过统一 config 接口传入配置
2. **工厂模式**：通过配置工厂创建 Three.js 对象实例
3. **生命周期管理**：Vue 生命周期对应 Three.js 对象创建/销毁
4. **观察者模式**：配置变更自动触发场景更新
5. **依赖注入**：Canvas 上下文向下注入到子组件

### 7.3 代码规范

- ESLint + Prettier 代码格式化
- 严格的 TypeScript 类型检查
- 统一的命名规范（PascalCase 组件，camelCase 函数）
- 单文件组件职责单一原则
- JSDoc 注释完整，配合自动生成文档

---

## 8. 限制条件和边界场景

### 8.1 性能边界

- 单个场景对象数量建议 < 1000 个
- 单模型面数建议 < 500k 三角面
- 移动设备配置降级策略

### 8.2 兼容性

- 浏览器：Chrome 90+, Firefox 88+, Safari 14+
- 不支持 IE 浏览器
- WebGL 2.0 作为最低要求

### 8.3 已知限制

- 复杂着色器需要自定义扩展
- 大型场景需要额外的性能优化处理
- 物理引擎功能存在性能开销
